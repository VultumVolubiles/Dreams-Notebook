server:
  port: 8080

spring:
  application:
    name: "Dreams Notebook"
  datasource:
    username: dev
    password: dev
    url: jdbc:h2:file:./database
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: classpath:/db/changelog/changelog.yaml
    enabled: true